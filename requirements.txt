# requirements.txt

# ── Core NLP / fuzzy ──
rapidfuzz>=3.9.0
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.12.0

# ── Color + utils ──
matplotlib>=3.7.0
webcolors>=1.11,<2

# ── NLP pipelines / ML ──
spacy>=3.7.0
nltk>=3.8.0
transformers>=4.40.0
torch>=2.1.0
tqdm>=4.65.0
requests>=2.31.0

# ── Dev tooling / static analysis ──
ruff>=0.6.0
mypy>=1.10.0

# ── Tests ──
pytest>=7.4.0
pytest-cov>=4.1.0
